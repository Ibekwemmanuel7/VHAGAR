# Notebooks

Exploratory only. Nothing here is on the promotion path, a result that exists
only in a notebook has no split manifest, no environment record, and no CI
coverage, so it cannot be reproduced or defended.

Suggested order:

1. `01_grid_and_tiles`, visualise the analysis grid over your AOI
2. `02_label_harmonisation`. MTBS / NBAC / EFFIS into the common event registry
3. `03_rbr_baseline`, the calibrated spectral-index baseline for T2
4. `04_split_design`, variogram-based block sizing for T3
5. `05_event_fusion`. GEO/LEO parallax tolerance sensitivity for T1
