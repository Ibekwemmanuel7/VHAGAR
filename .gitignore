__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
outputs/
mlruns/
data/
*.zarr/
*.tif
*.nc
.DS_Store
.env
.hypothesis/
*.tmp
mtbs_extract/
bsp_*.zip
emsr_extract/
EMSR*_vector.zip
emsr.csv
koppen_extract/
koppen_geiger_tif.zip
emsr_candidates.csv
emsr_full.csv

# Large data artifacts: never belong in git (GitHub rejects >100MB). Keep on disk.
*.zip
*.npz
*.parquet
viirs_truth.csv
serve/.cache/

# Exception: the small self-contained deploy snapshot IS committed so a hosted
# service (Render / container) starts with no raw data and no clustering.
!serve/demo/
!serve/demo/detections.parquet
